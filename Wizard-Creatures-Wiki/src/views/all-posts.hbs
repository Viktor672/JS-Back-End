{{setTitle "Catalog"}}

<section id="catalog">
    <h1>All posts</h1>
    <div class="band">
        {{#each wizard}}
        {{>wizard}}
        {{else}}
        <div class="no-posts">
            <p class="no-offer">There are no posts yet...</p>
        </div>
        {{/each}}
    </div>
</section>